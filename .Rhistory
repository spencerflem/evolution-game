chanceToEat <- computeChanceToEat(sampleSpace, values[,i])
print('0909090')
print(chanceToEat)
print('0909090')
#append row of matrix
}
#ensure 0-1 not 0-100
return(eatenMatrix)
}
stepSim(1,1,creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
summedCreatures <- rbind.fill(summedCreature)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
#print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
summedCreatures <- rbind.fill(summedCreature)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
#print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
summedCreatures <- rbind.fill(summedCreature)
}
return(summedCreatures)
}
print("IMAMORON")
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
#print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
summedCreatures <- rbind.fill(summedCreature)
}
print("IMAMORON")
return(summedCreatures)
}
summedCreatures(creatures)
print(summedCreatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
#print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
summedCreatures <- rbind.fill(summedCreature)
print(summedCreatures)
}
print("IMAMORON")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
#print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
print("--")
print("------")
summedCreatures <- rbind.fill(summedCreature)
print(summedCreatures)
print("-----")
print("--")
}
print("IMAMORON")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print(creature)
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
#print("--")
#print("------")
summedCreatures <- rbind.fill(summedCreature)
#print(summedCreatures)
#print("-----")
#print("--")
}
print("IMAMORON")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print("--")
print("------")
print(creature)
print("-----")
print("--")
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(summedCreature)
#print("--")
#print("------")
summedCreatures <- rbind.fill(summedCreature)
#print(summedCreatures)
#print("-----")
#print("--")
}
print("IMAMORON")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
print(summedCreatures)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(typeof(summedCreature))
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
print(summedCreatures)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
print(typeof(summedCreature))
summedCreatures <- rbind.fill.matrix(summedCreatures, summedCreature)
print(summedCreatures)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreatureFrame)
print(summedCreatures)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreatureFrame)
}
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
print(summedCreatureFrame)
summedCreatures <- rbind.fill(summedCreatures, summedCreatureFrame)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
print(summedCreatureFrame)
summedCreatures <- cbind.fill(summedCreatures, summedCreatureFrame)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
print(summedCreatureFrame)
summedCreatures <- colbind.fill(summedCreatures, summedCreatureFrame)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatureFrame <- as.data.frame(summedCreature)
print(summedCreatureFrame)
summedCreatures <- cbind(summedCreatures, summedCreatureFrame)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- list()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- c(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- list()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- list(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatrures
summedCreatrues
summedCreatures
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- list()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- list(c(summedCreatures, summedCreature))
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1])
summedCreatures <- rbind.fill(summedCreatures, as.data.frame(summedCreature))
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
shiny::runApp()
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print(typeof(creature))
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print(typeof(creature))
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
print(typeof(filledFrame))
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
print(typeof(summedCreature))
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
print(typeof(creature))
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
print(typeof(filledFrame))
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
summedCreature <- ldply(filledFrame[,1], sum)
print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
summedCreature <- ldply(filledFrame[,1], sum())
print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
summedCreature <- ldply(filledFrame[,1], sum) #.id = CREATURE ID?
print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
summedCreature <- ldply(filledFrame[,1], print) #.id = CREATURE ID?
print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
summedCreatures <- function(creatures) {
summedCreatures <- data.frame()
for (creature in creatures) {
filledFrame <- data.frame()
for(frame in creature) {
filledFrame <- rbind.fill(filledFrame, frame)
}
print(filledFrame)
summedCreature <- colSums(filledFrame[,-1]) #rm.na!!!
#summedCreature <- ldply(filledFrame[,1], print) #.id = CREATURE ID?
#print(summedCreature)
summedCreatures <- rbind.fill(summedCreatures, summedCreature)
}
print("OUT!")
return(summedCreatures)
}
summedCreatures(creatures)
